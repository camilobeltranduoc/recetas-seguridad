# JWT Configuration
# Generate a secure random key (min 256 bits for HS256, 384 bits for HS384)
# Example generation: openssl rand -hex 64
JWT_SECRET=your-super-secret-jwt-key-here-min-256-bits

# User passwords for initial data load
USER1_PASSWORD=secure_password_1
USER2_PASSWORD=secure_password_2
ADMIN_PASSWORD=secure_admin_password

# Database configuration (if using environment variables)
# SPRING_DATASOURCE_URL=jdbc:mysql://localhost:3306/recetas_db
# SPRING_DATASOURCE_USERNAME=recetas_user
# SPRING_DATASOURCE_PASSWORD=recetas_pass
